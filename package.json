{
  "name": "mcp-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Model Context Protocol (MCP) Monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "chat": "ENV_FILE_PATH=$(pwd)/.env pnpm --filter ./examples/chat run dev",
    "ai-sdk": "pnpm --filter ./examples/ai-sdk run start",
    "openai-sdk": "pnpm --filter ./examples/openai-sdk run start"
  },
  "keywords": [
    "mcp",
    "ai",
    "monorepo"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@ai-sdk/google": "^1.2.22",
    "lucide-react": "^0.446.0"
  }
}
